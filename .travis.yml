language: python
python:
  - "2.7"

#before_install:

# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: for mod in caom2visitor; do cd $mod; py.test --cov=$mod || break -1; cd ..; done
